## Packages
framer-motion | Animations and transitions
react-intersection-observer | For scroll-triggered animations

## Notes
- Theme provider uses standard Tailwind dark mode class.
- Images use Unsplash placeholders with descriptive comments.
- We assume standard Shadcn UI components are available in `client/src/components/ui/`.
- Font setup assumes `var(--font-sans)` is mapped in tailwind config, and we will define custom typography classes in `index.css`.
